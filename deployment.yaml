apiVersion: v1
kind: Namespace
metadata:
  name: hashiconf-1
  namespace: hashiconf-1
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hashiconf-1
  namespace: hashiconf-1
  labels:
    app: hashiconf-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashiconf-1
  template:
    metadata:
      labels:
        app: hashiconf-1
    spec:
      containers:
        - name: hashiconf-1
          image: codydearkland/ld-demo-hashiconf:1
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 3000
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  namespace: hashiconf-1
  name: hashiconf-lb
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: hashiconf-1
  type: LoadBalancer